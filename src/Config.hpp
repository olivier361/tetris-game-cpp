namespace Config {

    // These const variables determine some important parameters for the Tetris game.

    const char* appName = "Tetris"; // The name of the app to be displayed on the window header.

    const int windowSizeX = 1280; // the width of the graphics window in pixels.
    const int windowSizeY = 720;  // the height of the graphics window in pixels.

    const int blockSizePx = 30;   // the width and height of a block in pixels.

    const int playfieldBlockWidth = 10;   // the number of blocks for the grid width.
    const int playfieldBlockHeight = 20;  // the number of blocks for the grid height.
}